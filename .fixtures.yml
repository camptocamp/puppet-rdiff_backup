fixtures:
  forge_modules:
    buildenv: 'camptocamp-buildenv'
    concat: 'puppetlabs-concat'
    python: 'camptocamp-python'
    stdlib: 'puppetlabs-stdlib'
  symlinks:
    rdiff_backup: "#{source_dir}"
